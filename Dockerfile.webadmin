# Web Admin Dockerfile
FROM node:20-alpine

# Install git and other dependencies
RUN apk add --no-cache git

# Set working directory
WORKDIR /app

# Copy all webadmin files
COPY webadmin/ ./

# Install dependencies
RUN npm install

# Expose port
EXPOSE 8080

# Run the web admin
CMD ["npm", "start"]