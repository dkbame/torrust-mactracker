# Web Admin Dockerfile
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files first
COPY webadmin/package*.json ./

# Install dependencies
RUN npm install --production

# Copy the rest of the application
COPY webadmin/ ./

# Expose port
EXPOSE 8080

# Run the web admin
CMD ["npm", "start"]